name: Actions billing probe 2
on:
  workflow_dispatch:
jobs:
  macos:
    runs-on: macos-15
    steps:
      - run: sw_vers
  windows:
    runs-on: windows-latest
    steps:
      - run: echo "windows ok"
